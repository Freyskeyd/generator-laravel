/*global describe, it, beforeEach */
'use strict';
require('should');

describe('pool', function () {
  it('Should display a pool list', function  (done) {
    done();
  });
  it('Should display path list of a pool', function (done) {
    done();
  });
  it('Should add a pool to another pool (Inceptionnnn)', function (done) {
    done();
  });
  it('Should move a pool to another pool', function (done) {
    done();
  });
  it('Should remove a path/pool from config file', function (done) {
    done();
  });
  it('Should rename a path/pool', function (done) {
    done();
  });
});