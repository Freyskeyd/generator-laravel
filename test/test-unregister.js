/*global describe, it, beforeEach */
'use strict';

var laraCFG = require('../_lib'),
    _       = {};

describe('Register path', function () {

  beforeEach(function (done) {
    _ = new laraCFG();
    _.init(done);
  });

  it('Should unregister the current path', function (done) {

    done();
  });

  it('Should unregister the poolpath', function (done) {

    done();
  });
});