/*global describe, it, beforeEach */
'use strict';
require('should');

describe('doctor', function () {
  it('Should return an array of invalid path', function (done) {
    done();
  });
  it('Should return an array of valid path', function  (done) {
    done();
  });
  it('Should return status of migration for path/pool', function (done) {
    done();
  });
});