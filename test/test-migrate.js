/*global describe, it, beforeEach */
'use strict';
require('should');

describe('migrate', function () {
  it('Should return an array of valid path', function  (done) {
    done();
  });
  it('Should migrate a path/pool', function (done) {
    done();
  });
});