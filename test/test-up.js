/*global describe, it, beforeEach */
'use strict';
require('should');

describe('Down', function () {
  it('Should return an array of valid path', function  (done) {
    done();
  });
  it('Should define path/pool has up', function (done) {
    done();
  });
});